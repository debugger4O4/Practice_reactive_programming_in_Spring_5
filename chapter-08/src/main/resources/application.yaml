spring.cloud.stream:
  bindings:
    output: # ???? ????????.
      destination: Messages # ????????? ??????? ? org.springframework.cloud.stream.config.BindingProperties ? ?????????
                            # ??? ?????????? ? ??????? ?????????.
      producer: # ???????? ????????? ?????????????.
        requiredGroups: statistic, ui # ????????? ?????? ???????????, ??????? ?????? ???????? ????????? ? ?????????
                                      # ???????? ???? ?? ???? ???.

spring.cloud.stream:
  bindings:
    input: # ??????????? ????? input, ??????? ????????? ???????????? ???????????.
      destination: Messages
      group: statistic # ??????????? ??? ?????? ??????????? ? ??????? ?????????.
    output:
      producer:
        requiredGroups: ui
      destination: Statistic

spring.cloud.stream:
  bindings:
    statistic:
      destination: Statistic
      group: ui
    messages:
      destination: Messages
      group: ui
